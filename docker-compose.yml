version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: wusul-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: wusul_db
      POSTGRES_USER: wusul
      POSTGRES_PASSWORD: wusul_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wusul"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - wusul-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: wusul-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - wusul-network

  # Wusul API Application
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wusul-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      DATABASE_URL: postgresql://wusul:wusul_password@postgres:5432/wusul_db?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      # Add other environment variables from .env file
    ports:
      - "3000:3000"
    volumes:
      - ./prisma:/app/prisma
    networks:
      - wusul-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # pgAdmin (optional - for database management)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: wusul-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@wusul.com
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - wusul-network
    profiles:
      - tools

networks:
  wusul-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  pgadmin_data:
    driver: local
