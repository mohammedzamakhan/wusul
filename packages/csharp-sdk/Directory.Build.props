<Project>
  <PropertyGroup>
    <!-- Set different intermediate output paths based on project name to avoid conflicts -->
    <BaseIntermediateOutputPath Condition="'$(MSBuildProjectName)' == 'Wusul.Tests'">obj-tests\</BaseIntermediateOutputPath>
  </PropertyGroup>
</Project>
